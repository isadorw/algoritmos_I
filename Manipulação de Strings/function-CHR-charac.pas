// the function ord returns through a character its number in the ASCII table
// e.g: charac := CHR(number);

program ASCIICharac;

var 
    value: integer;
begin
    value := ord('A'); // in the ASCII table a is equal to 65
    writeln(value);
end.